# AI 功能施工计划

> 本文档是后续 AI 部分的执行约束。实施过程中按顺序推进；每个阶段开始前先复述该阶段步骤，结束时更新勾选状态和验证结果。不得跳过验收门槛。

## 0. 已锁定的范围

只实现以下四项能力：

1. 自然语言快速记任务
2. 一句话生成暑期计划
3. AI 调整计划
4. AI 每日复盘

第一版明确不做：通用聊天、语音、图片生成、知识库、社交、AI 自动写数据库、AI 自动修改整条重复规则。

## 1. 不可破坏的项目约束

- 现有 `services/store.js` 仍是数据读写和离线队列的唯一入口。
- 重复任务的实例编辑、取消和截断继续由 `services/task-service.js` 处理。
- 日期实例只能由 `services/model.js` 生成，AI 不负责计算完成率、连续天数或重复实例。
- AI 返回值中禁止出现或使用 `_openid`、数据库 `_id`；需要引用现有任务时使用本次请求生成的临时 `ref`。
- AI 只生成草稿或建议，必须经过本地校验和用户确认后才能写入数据。
- 日期必须满足 `vacation.startDate <= date <= vacation.endDate`。
- 重复类型只允许 `once`、`daily`、`weekly`，星期只允许整数 `0` 至 `6`。
- AI 调整第一版只执行实例级改期，不让 AI 拆分或改写整条重复规则。
- AI 不可用、超时、额度不足或离线时，原有手动功能必须继续可用。
- 不新增数据库集合；每日复盘第一版使用带数据指纹的本地缓存。
- 不在每次修改后上传 GitHub。每个阶段完成并通过测试后形成一个本地提交建议，由用户决定何时推送。

## 2. 防止臆测的工作规则

每个阶段严格执行下面的循环：

1. **读取事实**：先读将要修改的页面、服务、测试和实际字段，不凭记忆猜接口。
2. **列出本阶段步骤**：向用户说明本轮将改哪些文件、先后顺序和验收条件。
3. **最小改动**：一次只打通一条数据流，不同时开发两个尚未验证的 AI 功能。
4. **固定返回契约**：先写 schema 和测试样例，再连接模型，页面不得直接解析自由文本。
5. **执行验证**：运行领域测试、AI schema 测试、JavaScript 语法检查和 JSON 检查。
6. **人工验收**：给出开发者工具中的明确操作路径，由用户反馈截图或结果。
7. **记录状态**：在本文档中勾选已完成项、记录实际模型名和部署结果，再进入下一阶段。

遇到以下情况必须停下核对，不得自行编造替代方案：

- 控制台没有计划使用的模型或套餐不支持。
- 官方 SDK 的实际方法或返回结构与文档/本地依赖不一致。
- 模型无法稳定返回既定 JSON。
- 现有任务字段与本文记录的约束不一致。
- 需要新增集合、付费能力、隐私授权或公开发布资质。
- 测试失败且原因尚未查明。

## 3. 固定架构

```text
页面
  -> services/ai-service.js       请求状态、错误映射、调用云函数
  -> cloudfunctions/aiPlanner     身份、限流、提示词、模型调用
  -> services/ai-schema.js        客户端二次校验和规范化
  -> 预览/现有编辑器
  -> services/store.js / task-service.js
```

云函数使用单一入口，事件结构固定为：

```js
{
  action: 'parseTask' | 'generatePlan' | 'adjustPlan' | 'dailyReview',
  schemaVersion: 1,
  payload: {}
}
```

通用返回结构固定为：

```js
{
  ok: true,
  action: 'parseTask',
  schemaVersion: 1,
  data: {},
  usage: { inputTokens: 0, outputTokens: 0 },
  requestId: ''
}
```

失败统一返回可识别的错误码；页面不依赖厂商原始错误文案。

## 4. 实施顺序与门槛

### A0：基线审计与契约测试

- [ ] 记录当前 Git 提交和工作区状态。
- [ ] 核对 `vacation`、`goals`、`tasks`、`records` 的实际字段。
- [ ] 核对 `store.upsert()`、`store.rescheduleMany()` 和 `task-service` 的使用方式。
- [ ] 建立四种 AI 返回 schema 的纯 JavaScript 校验器测试。
- [ ] 建立合法、缺字段、错误日期、错误星期、额外字段、非 JSON 六类 fixture。

**门槛：** 不调用模型也能通过测试证明错误 AI 数据不会进入业务层。

### A1：云端 AI 最小链路

- [ ] 在控制台确认当前环境实际可用模型及其计费方式。
- [ ] 新建 `cloudfunctions/aiPlanner`，模型名只在一处配置。
- [ ] 按当前官方文档安装并锁定实际可用 SDK 版本，不使用未经验证的方法名。
- [ ] 仅实现 `parseTask` 的固定测试请求，验证身份、模型调用和 JSON 提取。
- [ ] 增加超时、并发锁、输入长度、最大输出和错误码映射。
- [ ] 部署后在开发者工具真实调用一次，记录实际响应结构。

**门槛：** 真正的云端请求成功；失败和超时也能返回稳定错误结构。未通过前不制作 AI 页面。

### A2：自然语言快速记任务

- [ ] 中央加号增加“手动添加 / AI 快速添加”，不改变原手动入口。
- [ ] 新增快速输入页面或面板，只负责输入和解析。
- [ ] `parseTask` 输出转换为现有任务字段：`title/date/time/note/goalId/repeat/repeatEnd/repeatToVacationEnd`。
- [ ] AI 只能匹配请求中提供的目标临时引用，不能生成目标 ID。
- [ ] 校验后跳转到现有 `task-edit` 预填表单，由用户最终保存。
- [ ] 覆盖单次、每天、每周、无时间、模糊日期、越界日期和离线场景。

**门槛：** AI 不直接保存；所有有效草稿都能由原任务编辑器保存，所有非法草稿都被拦截。

### A3：一句话生成暑期计划

- [ ] 新增计划输入页，输入目标、每天可用时间、休息日和强度。
- [ ] `generatePlan` 返回 `summary + goals + tasks`，目标与任务仅通过 `tempGoalRef` 关联。
- [ ] 新增结构化预览页，支持取消选择、编辑和重新生成。
- [ ] 导入前再次执行日期、重复规则、目标引用和每日负载校验。
- [ ] 使用现有 `store.upsert()` 顺序创建目标和任务，建立临时引用到真实 ID 的映射。
- [ ] 增加 `importFingerprint`，阻止同一生成结果重复导入。
- [ ] 云端保存失败时沿用现有离线队列，并明确显示等待同步。

**门槛：** 一次演示能从自然语言生成、预览、导入，并在今天页、计划页和报告页正确出现；重复点击不会产生两套数据。

### A4：AI 调整计划

- [ ] 只向 AI 发送未完成实例、未来每日负载、目标进度和假期边界。
- [ ] 为每个候选实例建立临时 `taskRef`，不发送数据库 ID。
- [ ] `adjustPlan` 只允许 `reschedule`，返回新日期、可选新时间和理由。
- [ ] 本地校验：原实例存在、未完成、未取消，新日期不在过去且不越界。
- [ ] 新增调整前后对比页，支持逐项选择和修改日期。
- [ ] 确认后统一调用 `store.rescheduleMany()`；不复制一套改期实现。
- [ ] 覆盖重复任务、已改期任务、部分采用、部分失败和重复提交。

**门槛：** 调整后同一实例只出现一次，过去记录、完成记录和重复规则保持不变。

### A5：AI 每日复盘

- [ ] 从统计服务提取当日摘要，不把整份数据库状态发给模型。
- [ ] `dailyReview` 固定返回：`summary`、`praise`、`improvement`、`tomorrowFocus`。
- [ ] 在今天页加入浅色复盘卡片，不改变主任务流程。
- [ ] 使用 `date + sourceHash + style` 缓存；数据未变化不重复调用。
- [ ] 支持温和、理性、严格三种提示风格，展示内容长度受限。
- [ ] 覆盖无任务、零完成、部分完成、全部完成和额度耗尽。

**门槛：** 不虚构具体任务；缓存命中不消耗 Token；复盘失败不影响今天页。

### A6：统一可靠性与展示验收

- [ ] 给四项入口统一 AI 标识、加载状态、错误状态和按钮锁。
- [ ] 检查所有滚动页面的底部安全区，不能被自定义导航遮挡。
- [ ] 增加服务器端调用频率限制和客户端短时间防抖。
- [ ] 记录 Token 用量但不记录用户完整输入和完整任务数据。
- [ ] 运行全部现有领域测试与新增 AI 测试。
- [ ] 运行全部 JavaScript 语法检查和 JSON 解析检查。
- [ ] 开发者工具完成四条主流程测试。
- [ ] 至少一台真机验证网络错误、云函数、布局和交互。
- [ ] 整理云端部署清单、测试清单和最终 Git 提交建议。

**最终完成定义：** 四项能力都能独立成功和独立失败；任一 AI 能力故障都不会损坏数据或阻塞原有手动功能。

## 5. 每阶段汇报模板

每个阶段结束后只按事实汇报：

```text
阶段：A?
完成项：
实际修改文件：
自动测试：通过/失败（列出命令和结果）
开发者工具待验证：
云函数状态：未部署/已部署
Token 调用：未发生/发生 N 次
遗留问题：
是否达到进入下一阶段的门槛：是/否
```

若门槛为“否”，下一轮继续当前阶段，不宣布进入下一阶段。
