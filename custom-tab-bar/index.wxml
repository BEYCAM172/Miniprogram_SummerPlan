<view class="tabbar">
  <view wx:for="{{list}}" wx:key="url" class="tab {{selected === index ? 'active' : ''}}" data-url="{{item.url}}" bindtap="switchTab">
    <text class="icon">{{item.icon}}</text><text>{{item.text}}</text>
  </view>
  <view class="fab" bindtap="add">＋</view>
</view>
